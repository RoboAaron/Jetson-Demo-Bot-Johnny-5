# PBI Backlog

This is the single source of truth for PBIs, ordered by priority.

| ID | Actor | User Story | Status | Conditions of Satisfaction (CoS) |
|----|--------|------------|--------|----------------------------------|
| 1 | Developer/User | As a developer, I want to implement core Teensy firmware for self-balancing control (using IMU and PID to ESCs via UART), Jetson interfacing via USB serial (reusing the power connection for bidirectional comms), and PS3 remote control via Jetson (temporary solution until ESP32 WROOM arrives), so that the robot can maintain stability, receive high-level commands, and be manually operated. | Agreed | - Teensy reads BNO085 IMU data and maintains balance via PID, sending current commands to dual FSESC over UART.<br>- Bidirectional USB serial communication with Jetson for IMU/sensor data upstream and velocity/steering commands downstream.<br>- PS3 controller handled by Jetson (USB or Bluetooth), sends velocity/steering commands to Teensy via USB serial. This avoids USBHost_t36 library conflict on Teensy.<br>- Safe blending of modes (e.g., remote inputs as PID setpoints).<br>- Unit tests for each component (e.g., IMU reading, PID stability, serial protocol); integration tests with Jetson and physical hardware.<br>- All changes logged in git with task-specific commits/PRs. |
| 2 | Developer/User | As a developer, I want to migrate BNO085 IMU from I2C to SPI interface, so that I can achieve 99% reliability with high-rate sensor updates (400-1000Hz) and low latency (<0.5ms) required for rock-solid balancing. | Rejected | - **ABANDONED**: SPI migration goal abandoned. Current I2C implementation at 400Hz provides sufficient performance for balancing. Focus shifted to cascaded control and PID tuning improvements instead. |
| 3 | Developer/User | As a developer, I want to implement tip-over boundaries and safety limits, so that the robot automatically stops or enters safe mode when tilt exceeds safe operating angles (e.g., ±30°) to prevent damage and ensure user safety. | Proposed | - Define maximum safe tilt angles (pitch/roll) based on mechanical limits.<br>- Implement tilt boundary detection in balance controller.<br>- Add automatic stop/safe mode when boundaries exceeded.<br>- Implement recovery routine to return to safe operating range.<br>- Add visual/audio feedback when approaching boundaries.<br>- Test boundary detection with physical tilting.<br>- Document safety limits and recovery procedures. |
| 4 | Developer/User | As a developer, I want to achieve rock-solid balancing (99%+ reliability) through systematic improvements including sensor fusion, velocity damping, LQR controller, and extensive testing, so that the robot maintains stability like commercial hoverboards/Segways. | InProgress | - ✅ **Phase 1 Cascaded Velocity Control**: Implemented velocity control loop (outer loop) with angle control (inner loop). Velocity PID runs at 20Hz with EMA filtering, deadband, output clamping, and slew rate limiting.<br>- 🔄 **Velocity Loop Fixes**: In progress - fixing velocity PID sign (REVERSE mode), deadband mode thrashing, and input clamping.<br>- ⏳ Implement sensor fusion (Kalman/complementary filter) for optimal angle estimation.<br>- ⏳ Add velocity damping (Kd_vel > 0) to eliminate chattering.<br>- ⏳ Optimize PID tuning for smooth operation (per `PATH_TO_99_PERCENT_RELIABILITY.md`).<br>- ⏳ Implement LQR controller for advanced control (optional, after PID optimization).<br>- ⏳ Conduct comprehensive testing across scenarios (smooth floor, uneven surfaces, inclines, disturbances, extended operation).<br>- ⏳ Achieve 99%+ success rate across all test scenarios.<br>- ⏳ Document tuning parameters and test results. |
| 5 | Developer/User | As a developer, I want to prioritize and plan the 8 core demo use cases (Vision-Driven Autonomy, Human Following, Conversational Companion, Object Recognition, Remote Teleoperation, Multi-Sensor Fusion, Voice Command, Safety + Recovery), so that I can implement them in a logical order that builds on previous capabilities. | Proposed | - Review all 8 demo use cases from `robotics_project_overview.md`.<br>- Analyze dependencies between demos (which require which).<br>- Prioritize demos based on complexity, dependencies, and value.<br>- Create implementation roadmap with sequence (e.g., 1, 2, 3...).<br>- Define prerequisites for each demo (hardware, software, integration requirements).<br>- Document prioritization rationale and implementation order. |
| 6 | Developer/User | As a developer, I want to build the aluminum chassis and rebalance the robot's center of mass, so that the robot has a rigid, crash-resistant frame with optimal weight distribution for stable balancing. | Proposed | - Fabricate/cut 3mm aluminum base plate per design specifications.<br>- Fabricate/cut top plate (6mm Lexan or 3mm aluminum - decision required).<br>- Assemble plate + standoff construction with proper deck stacking.<br>- Mount all components (batteries, FSESC, Jetson, Teensy, sensors) per design.<br>- Measure and verify center of mass height (<15cm ideal) and alignment over wheel axis.<br>- Verify symmetrical weight distribution.<br>- Test balance with new chassis and adjust component placement if needed.<br>- Document chassis assembly and CoG measurements. |
| 7 | Developer/User | As a developer, I want to create Isaac Sim models and simulations of the robot, so that I can test control algorithms, validate designs, and develop autonomy features in a safe virtual environment before deploying to hardware. | Proposed | - Create robot URDF/SDF model with accurate dimensions, mass properties, and joint configurations.<br>- Model sensors (IMU, camera, lidar) in Isaac Sim.<br>- Implement physics simulation with proper motor models and dynamics.<br>- Create simulation scenarios matching real-world test conditions.<br>- Validate simulation matches real robot behavior (tuning parameters).<br>- Use simulation for control algorithm development and testing.<br>- Document simulation setup and validation results. |
| 8 | Developer/User | As a developer, I want to implement Demo 1: Vision-Driven Autonomy (visual SLAM + Nav2), so that the robot can autonomously navigate using camera-based perception and mapping. | Proposed | - Integrate OAK-D Pro camera with ROS 2.<br>- Implement visual SLAM using SLAM toolbox or similar.<br>- Configure Nav2 for autonomous navigation.<br>- Test SLAM mapping in controlled environment.<br>- Test autonomous navigation to waypoints.<br>- Document SLAM configuration and navigation parameters. |
| 9 | Developer/User | As a developer, I want to implement Demo 2: Human Following + Gesture Control, so that the robot can detect and follow a person while responding to gestures. | Proposed | - Implement person detection using OAK-D Pro depth camera.<br>- Create human following algorithm (maintain distance, avoid obstacles).<br>- Implement gesture recognition (hand gestures for commands).<br>- Integrate with balance controller for smooth following motion.<br>- Test following behavior in various scenarios.<br>- Document gesture vocabulary and following parameters. |
| 10 | Developer/User | As a developer, I want to implement Demo 3: Conversational Companion (local ASR + LLM), so that the robot can understand voice commands and engage in conversation using on-device AI. | Proposed | - Integrate ReSpeaker microphone array with ROS 2.<br>- Implement Whisper ASR for speech-to-text (local processing).<br>- Integrate local LLM for conversation and command understanding.<br>- Create voice command interface for robot control.<br>- Test conversation quality and command recognition accuracy.<br>- Document ASR/LLM configuration and voice command vocabulary. |
| 11 | Developer/User | As a developer, I want to implement Demo 4: Object Recognition + Manipulation, so that the robot can identify objects and interact with them (if manipulator hardware exists). | Proposed | - Implement object detection using OAK-D Pro camera.<br>- Create object recognition pipeline (classification/identification).<br>- If manipulator exists: implement manipulation planning and execution.<br>- If no manipulator: implement object tracking and approach behaviors.<br>- Test object recognition accuracy and manipulation success rate.<br>- Document object recognition models and manipulation capabilities. |
| 12 | Developer/User | As a developer, I want to implement Demo 5: Remote Teleoperation (web/VR), so that the robot can be controlled remotely via web interface or VR headset. | Proposed | - Create web-based teleoperation interface (ROS 2 web bridge).<br>- Implement VR teleoperation support (if VR hardware available).<br>- Stream camera feed to remote interface.<br>- Send control commands from remote interface to robot.<br>- Test teleoperation latency and responsiveness.<br>- Document teleoperation setup and usage. |
| 13 | Developer/User | As a developer, I want to implement Demo 6: Multi-Sensor Fusion (IMU + camera + lidar), so that the robot can combine data from multiple sensors for robust perception and localization. | Proposed | - Integrate lidar sensor (RPLidar or similar) with ROS 2.<br>- Implement sensor fusion framework combining IMU, camera, and lidar data.<br>- Create unified perception pipeline with fused sensor data.<br>- Test fusion accuracy and robustness compared to individual sensors.<br>- Document sensor fusion architecture and calibration procedures. |
| 14 | Developer/User | As a developer, I want to implement Demo 7: Voice Command + Wake Word, so that the robot can wake from standby mode and respond to voice commands without continuous listening. | Proposed | - Implement wake word detection (e.g., "Hey Johnny" or similar).<br>- Integrate wake word with ASR pipeline (activate on wake word).<br>- Create standby mode that conserves power when not in use.<br>- Test wake word detection accuracy and false positive rate.<br>- Test voice command recognition after wake word activation.<br>- Document wake word configuration and voice command vocabulary. |
| 15 | Developer/User | As a developer, I want to implement Demo 8: Safety + Recovery (fall detection, auto-balance restore), so that the robot can detect dangerous situations and automatically recover to safe operation. | Proposed | - Implement fall detection using IMU data (rapid tilt changes, impact detection).<br>- Create recovery routine to restore balance after disturbance.<br>- Implement emergency stop procedures for various failure modes.<br>- Test fall detection accuracy and recovery success rate.<br>- Test recovery from various tilt angles and disturbances.<br>- Document safety procedures and recovery algorithms. |
| 16 | Developer/User | As a developer, I want to implement SLAM (Simultaneous Localization and Mapping) using LDROBOT STL-19P/D500 lidar sensor and SLAM Toolbox, so that the robot can build maps of its environment and localize itself within those maps for autonomous navigation. | Proposed | - Integrate LDROBOT STL-19P/D500 lidar sensor with ROS 2.<br>- Install and configure SLAM Toolbox for ROS 2.<br>- Mount lidar sensor per design specifications (6-8 inches above ground, horizontal orientation).<br>- Configure TF chain: map → odom → base_link → laser.<br>- Implement SLAM Toolbox with parameters tuned for balance robot dynamics.<br>- Test mapping in various environments (indoor, outdoor, different sizes).<br>- Test localization accuracy and loop closure detection.<br>- Document SLAM configuration, tuning parameters, and map storage procedures. |

## History
- 2025-09-28: PBI_1 created and transitioned to Proposed by AI_Agent (initial proposal).
- 2025-09-28: PBI_1 transitioned to Agreed by User (approval confirmed).
- 2025-01-XX: Backlog updated with PBIs 2-15 to capture all project requirements.
- 2025-01-XX: PBI 16 (SLAM) added as standalone use case for lidar-based mapping and localization.
- 2026-01-26: PBI 2 (SPI Migration) marked as Rejected - goal abandoned. PBI 4 status updated to InProgress - Phase 1 cascaded velocity control implemented. PBI 1 Task 1-2 marked as Done - IMU reading and PID balancing completed.
- 2026-01-28: PBI 4 Task 4-1 — motor direction fix in teensy_balance_cascaded.ino (RIGHT_MOTOR_DIRECTION_SIGN = -1.0) so both wheels spin same direction for balance.

## Notes
- **PBI 2 (SPI Migration)**: **ABANDONED** - Goal abandoned. Current I2C implementation at 400Hz provides sufficient performance. Focus shifted to cascaded control and PID tuning.
- **PBI 4 (Rock-Solid Balancing)**: **IN PROGRESS** - Building on PBI 1. Phase 1 cascaded velocity control implemented. Motor direction fixed (both wheels same direction for balance). Velocity loop sign and deadband issues in progress (Task 4-2).
- **PBI 5 (Use Case Prioritization)**: Should be completed before implementing demos (PBIs 8-15).
- **PBI 6 (Chassis Build)**: May affect balance tuning (PBI 4), consider sequencing.
- **PBI 7 (Isaac Sim)**: Can be done in parallel with hardware development, useful for testing.
- **PBI 16 (SLAM)**: Lidar-based SLAM foundational for navigation. Should be completed before or in parallel with PBI 8 (Vision-Driven Autonomy) which uses visual SLAM. Can be done after PBI 6 (Chassis Build) for lidar mounting.
- **PBIs 8-15 (Demos)**: Implementation order to be determined by PBI 5 prioritization.
