# [4-11] Tuning GUI — Performance and Responsiveness

**Parent**: [Tasks for PBI 4](./tasks.md)

## Description

Address multi-second lag in `robot_tuning_gui.py` (real-time plot and serial read path): unbounded plot history, synchronous heavy redraws, or per-line parsing blocking the UI thread.

## Status History

- 2026-04-16: Created as **Proposed** after operator report of >1 s GUI lag vs Teensy stream.

## Requirements

1. Profile or instrument the GUI update path (plot append + draw frequency).
2. Cap time history (e.g. last N seconds or N samples) and/or decimate for display.
3. Ensure serial read loop cannot backlog unbounded (drop or coalesce old lines if needed).
4. Document any new limits in GUI Help text.

## Implementation Plan

TBD at Agreed — likely changes in `tuning_code/robot_tuning_gui.py` only.

## Test Plan

Connect at 2 Mbaud, stream on for 5 min; UI latency stays under 200 ms subjective or measured frame time.

## Verification

Pending.

## Files Modified

Expected: `tuning_code/robot_tuning_gui.py`.
